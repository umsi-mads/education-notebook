language: python
python:
    - 3.6
sudo: required
services:
    - docker
install:
    - make test-env
script:
    # Tests currently failing, those were autogenerated by the cookiecutter.
    # We need to investigate further on those ones.
    # - make test
    - make build